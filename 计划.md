# 角色卡仓库 - 开发计划

## 一、技术架构

```
前端 SPA (HTML/CSS/JS)
        │
        ▼
Cloudflare Pages Functions (API)
        │
   ┌────┴────┐
   ▼         ▼
 Telegram   KV
(文件存储)  (元数据索引)
```

| 组件 | 用途 | 免费额度 |
|------|------|----------|
| **Cloudflare Pages** | 前端托管 + Functions API | 无限请求 |
| **Telegram Bot API** | 存角色卡原文件 + 聊天记录 | 无限存储 |
| **KV** | 卡片索引、标签、搜索 | 10万读取/天 / 1000写入/天 |
| **Environment Variables** | 密码认证 + Telegram 配置 | — |

---

## 二、核心参数

| 参数 | 值 |
|------|-----|
| 缩略图尺寸 | 200x200 |
| 文件大小限制 | 20MB |
| 分页默认 | 30 条/页 |
| 搜索方式 | KV 全量扫描 |
| 界面语言 | 仅中文 |
| 上传格式 | PNG + JSON |
| 上传方式 | 拖拽 + 粘贴 + 点击按钮 |
| 列表视图 | 网格 + 列表，可切换 |
| 分页方式 | 滚动加载 + 分页按钮，可切换 |
| 错误格式 | 统一 `{ok: true/false, data/error}` |
| 认证方式 | 环境变量密码 |

---

## 三、项目结构

```
character-card-vault/
├── wrangler.toml                      # Cloudflare 配置
├── functions/
│   ├── _middleware.js                  # CORS + 认证中间件
│   └── api/
│       ├── auth/
│       │   └── login.js               # POST 登录
│       ├── cards/
│       │   ├── index.js               # GET 列表 / POST 上传
│       │   ├── [id].js                # GET 详情 / PUT 更新 / DELETE 删除
│       │   └── [id]/
│       │       └── download.js        # GET 下载原始文件
│       ├── chats/
│       │   ├── index.js               # GET 列表 / POST 导入
│       │   ├── [id].js                # GET 详情 / PUT 更新 / DELETE 删除
│       │   └── [id]/
│       │       ├── messages.js        # PUT 更新单条消息
│       │       └── export.js          # GET 导出 .jsonl
│       ├── tags/
│       │   └── index.js               # GET 标签列表 / PUT 删除标签
│       └── export/
│           ├── batch.js               # POST 批量导出 ZIP
│           └── all.js                 # POST 全量备份 ZIP
├── public/
│   └── index.html                     # 前端单页应用
├── opencode.json                      # OpenCode 配置（含 ponytail 插件）
├── .gitignore
├── README.md
└── LICENSE                            # CC BY-NC-SA 4.0
```

### ponytail 插件

使用 [ponytail](https://github.com/DietrichGebert/ponytail) 优化代码生成，遵循 YAGNI 原则：
- 优先使用浏览器原生 API（`<input type="date">`、`structuredClone()`、`crypto.subtle`、`URL`/`URLSearchParams`）
- 避免不必要的依赖（不装 lodash、moment 等）
- 一行能解决的不写两行
- 保持验证、错误处理、安全性不缩水

安装方式（`opencode.json`）：
```json
{
  "plugin": ["@dietrichgebert/ponytail"]
}
```

本项目中 ponytail 的具体应用：
- **前端**：使用原生 `fetch`、`crypto.subtle.digest`、`IntersectionObserver`、CSS 变量
- **API**：使用原生 `Request`/`Response`、`URLSearchParams` 解析参数
- **图片处理**：使用原生 Canvas API 生成缩略图
- **压缩**：使用原生 `CompressionStream`（如浏览器支持）或最小化依赖

---

## 四、数据模型

### KV 索引结构（key = `card:{id}`）

```json
{
  "id": "sha256_fingerprint",
  "name": "角色名",
  "creator": "作者",
  "spec": "chara_card_v2",
  "character_version": "1.0",
  "tags": ["原创", "女性"],
  "userTags": ["我的收藏"],
  "description": "前200字摘要（搜索用）",
  "personality": "前200字摘要",
  "scenario": "前200字摘要",
  "first_mes": "前200字摘要",
  "mes_example": "前200字摘要",
  "system_prompt": "前200字摘要",
  "world": "世界书关键词摘要",
  "r2Key": "cards/sha256.png",
  "thumbKey": "thumbs/sha256_thumb.png",
  "fileSize": 123456,
  "importedAt": 1700000000000,
  "favorited": false
}
```

### R2 存储结构

```
cards/
  └── {fingerprint}.png           # 原始角色卡文件
thumbs/
  └── {fingerprint}_thumb.png     # 缩略图（200x200）
chats/
  └── {cardId}/
      └── {chatId}.jsonl
```

---

## 五、API 设计

### 认证

```
POST /api/auth/login
  body: { password: "xxx" }
  response: { ok: true, data: { token: "xxx" } }
```

### 角色卡

```
GET /api/cards
  ?page=1&limit=30
  &sort=new|old|az
  &tag=标签名
  &creator=作者名
  &favorite=true
  &hasChat=true
  &scope=all|description|personality|scenario|first_mes|mes_example|system_prompt|world
  &q=关键词
  response: { ok: true, data: [...], total: 123, page: 1, limit: 30 }

POST /api/cards
  multipart/form-data
  file: PNG/JSON 文件
  response: { ok: true, data: { id, name, ... } }

GET /api/cards/:id
  response: { ok: true, data: { ...完整卡片信息, thumbUrl, fileUrl } }

PUT /api/cards/:id
  body: { tags, userTags, favorited }
  response: { ok: true }

DELETE /api/cards/:id
  response: { ok: true }

GET /api/cards/:id/download
  response: 原始 PNG 文件流
```

### 聊天记录

```
GET /api/chats?cardId=xxx
  response: { ok: true, data: [...] }

POST /api/chats
  multipart/form-data
  file: .jsonl/.json 文件
  cardId: 关联的卡片 ID
  response: { ok: true, data: { id, title, msgCount, ... } }

GET /api/chats/:id
  response: { ok: true, data: { ...聊天记录, messages: [...] } }

PUT /api/chats/:id
  body: { title }
  response: { ok: true }

PUT /api/chats/:id/messages
  body: { messages: [...] }
  response: { ok: true }

DELETE /api/chats/:id
  response: { ok: true }

GET /api/chats/:id/export
  response: .jsonl 文件下载
```

### 标签

```
GET /api/tags
  response: { ok: true, data: ["标签1", "标签2", ...] }

PUT /api/tags
  body: { action: "delete", tag: "标签名" }
  response: { ok: true }
```

### 导出

```
POST /api/export/batch
  body: { cardIds: ["id1", "id2", ...] }
  response: ZIP 文件下载

POST /api/export/all
  response: 全量备份 ZIP 下载
```

### 错误响应格式

```json
// 成功
{ "ok": true, "data": { ... } }

// 错误
{ "ok": false, "error": "错误信息" }
```

---

## 六、UI 设计方案

使用 `frontend-design` skill 设计，打造独特的"数字收藏馆"风格。

### 设计理念

| 维度 | 方向 |
|------|------|
| **目的** | 个人角色卡管理工具，类似"数字收藏柜" |
| **调性** | 暗色奢华 + 复古未来感，像一个精致的收藏馆 |
| **差异化** | 原版是工具感，我们要做出"收藏品"的质感 |
| **记忆点** | 卡片悬浮时的 3D 翻转效果 + 粒子背景 |

### 视觉风格

| 元素 | 设计 |
|------|------|
| **配色** | 深空黑底 + 琥珀金点缀 + 冷灰层次 |
| **字体** | 标题用几何感强的 Display 字体，正文用优雅的无衬线 |
| **卡片** | 毛玻璃质感 + 微妙内阴影 + 悬浮发光边框 |
| **背景** | 动态渐变网格 + 噪点纹理 |
| **动效** | 卡片入场错落 + 悬浮提升 + 详情页滑入 |

### CSS 变量定义

```css
:root {
  /* 深空黑底 + 琥珀金 */
  --bg-deep: #0a0c10;
  --bg-surface: #12151c;
  --bg-card: rgba(18, 21, 28, 0.8);
  --accent: #d4a853;
  --accent-glow: rgba(212, 168, 83, 0.3);
  --text-primary: #e8e6e3;
  --text-secondary: #8a8f98;
  --border: rgba(255, 255, 255, 0.06);
  --glass: rgba(255, 255, 255, 0.03);
  
  /* 亮色主题 */
  --bg-deep-light: #f5f3f0;
  --bg-surface-light: #ffffff;
  --accent-light: #b8860b;
}
```

### 关键动效

1. **卡片入场**：从下方淡入 + 错落延迟（stagger）
2. **悬浮效果**：Y 轴上移 + 边框发光 + 阴影扩大
3. **详情页**：从右侧滑入 + 背景模糊
4. **主题切换**：CSS 变量过渡动画

---

## 七、前端功能清单

### 核心功能
- [x] 角色卡网格/列表视图切换
- [x] 滚动加载 + 分页按钮
- [x] 搜索（名字/作者/描述/性格/场景/世界书）
- [x] 搜索范围筛选（全部/描述/性格/...）
- [x] 排序（最新/最旧/A-Z）
- [x] 标签云筛选
- [x] 收藏筛选
- [x] 有聊天记录筛选
- [x] 导入（拖拽 + 粘贴 + 点击）
- [x] 导入进度条
- [x] 导入失败重试
- [x] 去重检测

### 详情页
- [x] 角色卡详情页
- [x] 备选开场白 tab 切换
- [x] 世界书折叠展示
- [x] 扩展字段 JSON 折叠
- [x] 标签管理（添加/删除）
- [x] 收藏切换
- [x] 字段一键复制
- [x] 导出单卡 PNG

### 聊天记录
- [x] 导入聊天记录（.jsonl/.json）
- [x] 查看消息列表（用户/AI 分色气泡）
- [x] 在线编辑消息
- [x] 搜索消息内容
- [x] 消息统计（数量、比例、时间分布）
- [x] 导出 .jsonl

### 批量操作
- [x] 多选模式 + 全选
- [x] 批量删除
- [x] 批量添加标签
- [x] 批量导出 ZIP
- [x] 全量备份导出

### 其他
- [x] 暗色/亮色主题切换
- [x] 密码登录页
- [x] 响应式设计（移动端适配）
- [x] 懒加载头像
- [x] 角色卡预览浮窗

---

## 八、开发步骤

| 步骤 | 内容 | 优先级 | 说明 |
|------|------|--------|------|
| **0** | **安装 ponytail 插件** | 高 | 配置 `opencode.json`，让后续代码生成更精简 |
| 1 | 使用 `frontend-design` skill 设计 UI | 高 | 生成完整的 HTML/CSS 设计稿，确定视觉风格、配色、字体、动效 |
| 2 | 项目结构 + wrangler.toml | 高 | 创建目录结构和 Cloudflare 配置 |
| 3 | 认证中间件 + 登录 API | 高 | `_middleware.js` + `auth/login.js` |
| 4 | 角色卡上传/解析 API | 高 | PNG 解析 + 缩略图生成 + R2/KV 写入 |
| 5 | 角色卡列表/详情/删除 API | 高 | KV 查询 + 分页 + 筛选 |
| 6 | 前端：角色卡列表页 | 高 | 基于步骤1的设计稿实现 |
| 7 | 前端：角色卡详情页 | 高 | 基于步骤1的设计稿实现 |
| 8 | 前端：导入功能 | 高 | 拖拽 + 粘贴 + 点击上传 |
| 9 | 聊天记录 API | 高 | 导入/查看/编辑/删除/导出 |
| 10 | 前端：聊天记录模块 | 高 | 消息气泡 + 编辑 + 搜索 + 统计 |
| 11 | 标签管理 API + 前端 | 中 | 标签 CRUD + 标签云 |
| 12 | 批量操作 + 导出 API | 中 | 批量删除/导出 ZIP |
| 13 | 前端：批量操作 + 主题切换 | 中 | 多选模式 + 暗亮主题 |
| 14 | 前端：全量备份导出 | 中 | 全量 ZIP 下载 |
| 15 | .gitignore + README.md + LICENSE | 高 | 项目文档 |
| 16 | 测试 + 修复 | 高 | 功能测试 + UI 调优 |

---

## 九、环境变量配置

```toml
# wrangler.toml
name = "character-card-vault"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
# ACCESS_PASSWORD = "your-password-here"  # 部署时在 Cloudflare Dashboard 设置

[[r2_buckets]]
binding = "CARDS_BUCKET"
bucket_name = "character-cards"

[[kv_namespaces]]
binding = "CARDS_KV"
id = "your-kv-namespace-id"

[site]
bucket = "./public"
```

---

## 十、部署步骤

### 方式一：通过 Cloudflare 控制台部署（推荐）

#### 1. 准备工作

1. **注册 Cloudflare 账号**
   - 访问 https://dash.cloudflare.com 注册
   - 完成邮箱验证

2. **创建 Telegram Bot**
   - 在 Telegram 中搜索 @BotFather
   - 发送 `/newbot` 命令
   - 按提示输入 Bot 名称和用户名
   - 保存获得的 **Bot Token**

3. **创建 Telegram 频道**
   - 在 Telegram 中创建一个新频道（公开或私有）
   - 将刚才创建的 Bot 添加为频道管理员
   - 保存频道的 **Chat ID**（可通过 @VersaToolsBot 或 @GetTheirIDBot 获取）

4. **Fork 本仓库到 GitHub**
   - 访问本仓库页面，点击右上角 "Fork"
   - 选择你的 GitHub 账号
   - 等待 Fork 完成

#### 2. 创建 KV 命名空间

1. 登录 Cloudflare 控制台
2. 左侧菜单选择 **Workers 和 Pages**
3. 点击 **KV** 标签
4. 点击 **创建命名空间**
5. 配置：
   - **命名空间名称**：`character-cards-kv`
6. 点击 **添加命名空间**
7. 记录 **命名空间 ID**（一串字母数字），后续配置需要

#### 3. 连接 GitHub 仓库

1. 在 Cloudflare 控制台左侧菜单选择 **Workers 和 Pages**
2. 点击 **创建应用程序**
3. 选择 **Pages** 标签
4. 点击 **连接到 Git**
5. 选择你的 GitHub 账号
6. 选择刚 Fork 的仓库 `character-card-vault`
7. 配置构建设置：
   - **生产分支**：`main`（或 `master`）
   - **构建命令**：留空（不需要构建）
   - **构建输出目录**：`public`
8. 点击 **保存并部署**
9. 等待部署完成（约 1-2 分钟）

#### 4. 设置环境变量

1. 进入刚创建的 Pages 项目
2. 点击 **设置** 标签
3. 左侧选择 **环境变量**
4. 在 **生产** 部分点击 **添加变量**
5. 添加以下变量：
   - **变量名称**：`ACCESS_PASSWORD`，**值**：设置你的访问密码
   - **变量名称**：`TG_BOT_TOKEN`，**值**：你的 Telegram Bot Token
   - **变量名称**：`TG_CHAT_ID`，**值**：你的 Telegram 频道 Chat ID
6. 点击 **保存**

#### 5. 绑定 KV 命名空间

1. 在 Pages 项目页面，点击 **设置** 标签
2. 左侧选择 **集成**
3. 找到 **KV 命名空间绑定** 部分，点击 **添加绑定**
4. 配置：
   - **KV 命名空间**：选择 `character-cards-kv`
   - **变量名称**：`CARDS_KV`
5. 点击 **添加绑定**

#### 6. 重新部署

1. 回到 Pages 项目概览页
2. 点击 **重试部署** 或等待自动重新部署
3. 部署完成后，点击 **访问站点** 访问你的角色卡仓库

#### 7. 首次访问

1. 打开部署后的网站
2. 输入你设置的访问密码
3. 开始使用！

---

### 方式二：通过 Wrangler CLI 部署

#### 1. 安装 Wrangler CLI

```bash
npm install -g wrangler
```

#### 2. 登录 Cloudflare

```bash
wrangler login
```

按照提示在浏览器中完成登录授权。

#### 3. 创建 KV 命名空间

```bash
# 创建生产环境 KV
wrangler kv namespace create CARDS_KV

# 记录输出的 ID，修改 wrangler.toml 中的 id
```

#### 4. 修改 wrangler.toml

将 `wrangler.toml` 中的 `id = "your-kv-namespace-id"` 替换为上一步获取的实际 ID。

#### 5. 设置环境变量

```bash
# 创建 .dev.vars 文件（本地开发用）
echo 'ACCESS_PASSWORD="your-password-here"' > .dev.vars
echo 'TG_BOT_TOKEN="your-bot-token"' >> .dev.vars
echo 'TG_CHAT_ID="your-chat-id"' >> .dev.vars

# 生产环境需要在 Cloudflare 控制台设置
```

#### 6. 本地开发测试

```bash
wrangler pages dev public
```

访问 http://localhost:8788 测试功能。

#### 7. 部署到生产环境

```bash
wrangler pages deploy public
```

#### 8. 设置生产环境变量

1. 访问 https://dash.cloudflare.com
2. 进入 Workers 和 Pages > 你的项目 > 设置 > 环境变量
3. 添加 `ACCESS_PASSWORD`、`TG_BOT_TOKEN`、`TG_CHAT_ID` 变量
4. 绑定 KV（参考方式一的步骤 5）

---

### 常见问题

**Q1: 上传角色卡时提示"无法从 PNG 文件中解析角色卡数据"**
- 原因：角色卡 PNG 文件中没有嵌入正确的元数据
- 解决：确保角色卡是标准的 SillyTavern 格式，包含 `chara` 或 `ccv3` 数据块

**Q2: 上传时提示"Telegram 配置未设置"**
- 原因：环境变量未正确配置
- 解决：检查环境变量中是否添加了 `TG_BOT_TOKEN` 和 `TG_CHAT_ID`

**Q3: 图片无法显示**
- 原因：Telegram Bot 不是频道管理员，或 Chat ID 错误
- 解决：确保 Bot 已添加为频道管理员，且 Chat ID 正确

**Q4: 搜索功能不工作**
- 原因：KV 绑定未正确配置
- 解决：检查 KV 绑定名称是否为 `CARDS_KV`，确认 KV 命名空间 ID 与 wrangler.toml 一致

**Q5: 部署后无法登录**
- 原因：环境变量未设置或未生效
- 解决：检查环境变量中是否添加了 `ACCESS_PASSWORD`，确认变量是在生产环境设置的，重新部署一次

**Q6: 如何查看日志排查问题？**
- 解决：进入 Pages 项目 > Functions 标签，查看实时日志或下载日志文件

---

### 部署检查清单

- [ ] Cloudflare 账号已注册并验证
- [ ] Telegram Bot 已创建，Bot Token 已保存
- [ ] Telegram 频道已创建，Bot 已添加为管理员
- [ ] 频道 Chat ID 已获取
- [ ] 仓库已 Fork 到 GitHub
- [ ] KV 命名空间已创建
- [ ] GitHub 仓库已连接到 Cloudflare Pages
- [ ] 环境变量 `ACCESS_PASSWORD` 已设置
- [ ] 环境变量 `TG_BOT_TOKEN` 已设置
- [ ] 环境变量 `TG_CHAT_ID` 已设置
- [ ] KV 绑定 `CARDS_KV` 已添加
- [ ] 部署成功完成
- [ ] 网站可正常访问
- [ ] 登录功能正常
- [ ] 角色卡上传/下载功能正常

---

## 十一、LICENSE

CC BY-NC-SA 4.0
