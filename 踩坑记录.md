# 踩坑记录

> 本项目犯过的错误与解决方法，防止重蹈覆辙。每次踩坑后追加一条。
> 条目格式：**症状 → 根因 → 解法 → 预防**。

---

## 部署 / Cloudflare

### 1. 中间件放错位置，整站 401
- **症状**：部署后页面静态资源全部 401，需登录才能加载 CSS/JS
- **根因**：`_middleware.js` 放在 `functions/` 根目录，作用域覆盖全站（含静态资源）
- **解法**：移入 `functions/api/`，只拦截 API 路径
- **预防**：Pages Functions 的中间件只对同目录及子目录生效，放之前先想清楚作用域

### 2. wrangler.toml 缺 pages_build_output_dir
- **症状**：Pages 构建报错，找不到输出目录
- **根因**：wrangler.toml 必须显式声明 `pages_build_output_dir`（顶层）
- **解法**：加上 `pages_build_output_dir = "./public"`
- **预防**：改 wrangler.toml 后本地 `wrangler pages dev` 先跑一遍再提交

### 3. 环境变量"仪表板加不了"（wrangler.toml 接管）
- **症状**：仪表板提示"环境变量在通过 wrangler.toml 进行管理，仅机密（加密变量）可以通过仪表板管理"
- **根因**：仓库提交 wrangler.toml 后，Cloudflare 把普通明文变量的管理权切给该文件
- **解法**：变量分两类——机密（`ACCESS_PASSWORD`/`TG_BOT_TOKEN`/`TG_CHAT_ID`）走仪表板"机密"类型；非敏感（`SITE_NAME` 等）写 wrangler.toml 的 `[vars]`
- **预防**：机密**永远不写进会提交的文件**；改部署方式前查 Cloudflare 当前行为，别凭旧经验

### 4. KV ID 是账号专属值
- **症状**：Fork 者直接部署报 `Invalid KV namespace ID`
- **根因**：wrangler.toml 里的 KV 命名空间 ID 绑定原作者账号
- **解法**：文档（DEPLOY.md）把"替换 KV ID"列为 Fork 后必改步骤 + FAQ 对应条目
- **预防**：配置文件里任何账号专属值都要在部署文档中标注"必改"

---

## 前端

### 5. 上传弹窗状态残留（本次最典型）
- **症状**：导入下一张卡时，弹窗仍显示上一批的"已选择"界面，且点击选择文件无反应
- **根因**：三连——① `handleFiles` 用 `innerHTML` 整体替换拖拽区后从不恢复；② 重开弹窗只重置进度条不清 `selectedFiles`；③ `fileInput.value` 不清空导致同名文件不触发 `change` 事件。残留的旧列表还会让"确认上传"重复上传上一批
- **解法**：新增 `resetUploadModal()` 在弹窗**唯一入口**（打开按钮）统一重置全部状态；选中后立即清 `fileInput.value`；无效选择分支也恢复初始视图
- **预防**：凡是 `innerHTML` 替换式 UI，必须配套"重置函数 + 打开时调用"；`input type="file"` 用完就清 `.value`；模态框重置放在入口处单点做，不要散在关闭/上传等多处

### 6. img 标签带不了 Authorization
- **症状**：缩略图 `<img src="/api/cards/:id/thumb">` 返回 401
- **根因**：`<img>` 无法携带自定义请求头
- **解法**：`fetch` 带 Authorization 取 blob → `URL.createObjectURL`，并做本地缓存
- **预防**：受保护的二进制资源别指望 img 直连，走 fetch+blob

### 7. 中文文件名下载乱码
- **症状**：下载的卡文件名乱码
- **根因**：`atob` 直接解 Base64 的 UTF-8 内容会按字节错切
- **解法**：`TextDecoder` 解码 + `Content-Disposition` 用 RFC 5987（`filename*=UTF-8''...`）
- **预防**：HTTP 头里的非 ASCII 一律 RFC 5987，Base64 解码后当字节流处理

### 8. API 返回非 JSON 时的晦涩报错
- **症状**：服务端 502 返回 HTML，前端报 "Unexpected token < in JSON" 用户看不懂
- **根因**：`response.json()` 对非 JSON 直接抛语法错误，绕过了自定义错误处理
- **解法**：`.json().catch(() => ({ ok: false, error: '服务返回异常' }))`
- **预防**：解析外部响应永远假设它可能不是预期格式

### 9. URL 参数残留导致搜索永远空结果
- **症状**：带旧搜索范围参数（如已删除的 `scope=first_mes`）的链接打开后搜索无结果
- **根因**：恢复 URL 状态时不校验值是否仍合法
- **解法**：恢复前校验 scope 在当前下拉选项中，非法回退默认
- **预防**：URL 持久化的状态 = 不可信输入，恢复时逐项校验

---

## 后端 / 数据

### 10. 删功能残留
- **症状**：移除聊天记录功能后担心代码/文档有残留
- **根因**：删功能往往漏掉隐藏关联（文档、CSS、筛选参数、导出导入字段）
- **解法**：全库 grep 功能关键词（含 README/DEPLOY/字段名），归档完整版到独立分支
- **预防**：删功能 = 代码 + 文档 + 数据字段 + 测试四处一起清，grep 验证零残留

### 11. 新字段对旧数据的兼容
- **症状**：新增 `greetingCount`/`userTags` 后，旧卡无此字段
- **根因**：KV 里的旧数据不会自动长出新字段
- **解法**：读取处全部兜底（`(card.userTags || [])`），文档注明"旧卡需重传才享受新解析"
- **预防**：给已有存储加字段时，先 grep 所有读取点补兜底，别指望存量数据自己迁移

### 12. 死代码混入提交
- **症状**：`parseCharacterCard` 里留着从未使用的 `textChunks` 数组
- **根因**：开发中途换了实现思路，旧变量没删干净
- **解法**：审查时发现并删除
- **预防**：提交前通读 diff，看到"赋值后从未读取"的变量就删

---

## 流程 / 工具

### 13. 本地敏感文件被提交过
- **症状**：`计划.md`、`opencode.json`、`接口定义/` 曾进入仓库历史（后移除），`发帖.md` 险些跟进
- **根因**：新建本地文件时没第一时间加 .gitignore
- **解法**：逐个移除并加入 .gitignore；历史抽查确认无隐私
- **预防**：**新建任何不想公开的文件，第一动作就是加 .gitignore**，而不是"记得别提交"

### 14. 改了功能忘同步文档
- **症状**：搜索维度改名后 README 漏更新（提交时才发现 README 未入库）
- **根因**：文档同步靠记忆
- **解法**：AGENTS.md 写入硬规则：功能/交互/环境变量/部署步骤变化 → 必须同步 README/DEPLOY
- **预防**：把文档同步变成提交前检查清单的一部分，不靠自觉

### 15. 提交漏文件
- **症状**：一次提交只 stage 了代码，README 留在工作区
- **根因**：`git add` 按记忆列文件，没对照 `git status`
- **解法**：补提交
- **预防**：提交前 `git status --short`，改动的文件和 stage 清单一一核对

### 16. PowerShell 控制台乱码误判
- **症状**：验证 API 返回的中文时控制台显示乱码，差点误判为数据错误
- **根因**：Windows 控制台 GBK 编码显示 UTF-8 内容，是**显示问题**不是数据问题
- **解法**：把响应写入临时文件再读，按字节验证
- **预防**：控制台看到的乱码先怀疑终端编码，写文件/换 UTF-8 输出再下结论

### 17. 正则验证被嵌套结构截断
- **症状**：用正则提取 HTML 片段对比，非贪婪匹配在嵌套 `</div>` 处提前截断，误以为内容缺失
- **根因**：正则不适合配对嵌套标签
- **解法**：改用关键行内容计数验证（每行内容应出现 2 次）
- **预防**：验证 HTML/嵌套结构用内容匹配计数，别用正则抓整块
